# Tasks

## 1. Ядро

- [x] 1.1 Разбор описания структуры в `packages/core/src/structure.ts`: правила `file`, строгая папка, `"*"`, `any`, `?`, шаблоны имён, `ignore`, ошибки описания со строками; проверка — тесты на каждое правило и на каждую ошибку описания, тест чистоты `core` проходит
- [x] 1.2 Проверка соответствия по функции чтения каталогов: нет элемента, лишний, не тот тип, нестрогий корень, приоритет точного имени над шаблоном, дерево с отметками; проверка — тесты на таблице каталогов в памяти для каждого сценария спеки

## 2. Бэкенд

- [x] 2.1 `StructureService`: чтение `openspec/structure.yaml` с номерами строк, обход файловой системы, создание описания по текущей раскладке `openspec/`, маршруты `GET /api/structure` и `POST /api/structure/init`, наблюдение за папками верхнего уровня; проверка — тесты сервиса на временном проекте (строки правил, отсутствие описания, созданное описание проходит проверку на фикстуре) и тест маршрутов через `inject`

## 3. Интерфейс

- [x] 3.1 Раздел «Структура»: итог, нарушения с переходом, ошибки описания, дерево с отметками, создание описания, перепроверка по событию изменения файлов; проверка — e2e: создание описания даёт «соответствует», лишний файл в строгой папке появляется без действий пользователя

## 4. VS Code

- [x] 4.1 Перевод нарушений в диагностики, команда «Проверить структуру папок», проверка при активации, по событиям бэкенда и по созданию/удалению файлов, раздел «Структура» в протоколе панели; проверка — модульный тест перевода, дымовой тест: лишний файл даёт диагностику на файле, отсутствующий — на строке `structure.yaml`

## 5. Документация и макеты

- [x] 5.1 README: формат `openspec/structure.yaml` с примерами жёсткой и мягкой структуры; экран «Структура» в макетах; проверка — `npm run verify` и `npm run test:e2e` проходят
