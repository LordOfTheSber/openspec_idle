# Tasks

## 1. Бэкенд и протокол

- [ ] 1.1 Описать протокол сообщений панели в `packages/core/src/hostProtocol.ts`: запрос, ответ, событие, открытие файла, навигация, проверка входящих сообщений; проверка — тесты отклоняют сообщение неизвестного вида, запрос без идентификатора и запрос к пути вне `/api/`, а тест чистоты `core` проходит
- [ ] 1.2 Реализовать `createEmbeddedBackend()` в `packages/server` поверх `createApp()` и `inject()`: без сокета и без раздачи страницы, с наблюдателем, подпиской на события и закрытием; проверка — тесты на фикстуре получают дерево рабочего пространства, 409 с версией диска при конфликте записи, событие `workspace-changed` после правки файла и подтверждают, что после `close()` событий больше нет

## 2. Транспорт интерфейса

- [ ] 2.1 Вынести транспорт API в `packages/web/src/lib/transport.ts` с реализациями `fetch` и `postMessage`, перевести на него `get`/`send`; проверка — модульный тест транспорта `postMessage` сопоставляет ответы запросам по идентификатору и превращает 409 с версией диска в `StaleWriteConflict`, e2e HTTP-режима проходят без изменений
- [ ] 2.2 Добавить `lib/host.ts`: определение хоста, поток событий из сообщений, открытие файлов и навигация от хоста; в режиме VS Code скрыть раздел «Обозреватель» и открывать артефакты, результаты поиска и элементы метрик в редакторе VS Code; проверка — модульный тест потока событий из сообщений, сборка `web` без ошибок типов
- [ ] 2.3 Собирать интерфейс с относительными путями и оформлять его токенами темы VS Code в режиме хоста; проверка — в собранном `index.html` нет абсолютных путей `/assets/`, e2e HTTP-режима проходят

## 3. Расширение VS Code

- [ ] 3.1 Завести пакет `packages/vscode`: манифест с контейнером панели действий, деревом, командами, меню и событием активации, сборку esbuild в `dist/extension.cjs`, копирование интерфейса в `media/web`; проверка — `npm run build` собирает бандл, а бандл загружается дымовым тестом с подменённым модулем `vscode`
- [ ] 3.2 Реализовать выбор корня по папкам рабочей области и состояния «не инициализирован» и «нет CLI»; проверка — модульные тесты на папку с `openspec/`, на подкаталог проекта и на рабочую область без OpenSpec
- [ ] 3.3 Реализовать модель узлов дерева по `WorkspaceTree` и `TreeDataProvider` с обновлением по событию изменения файлов; проверка — модульные тесты на собственную схему (артефакты этой схемы), на прогресс `4/11` и на вывод числа ошибок вместо прогресса, на вложенные capability
- [ ] 3.4 Открывать артефакты, спеки и схемы в редакторе VS Code, отсутствующий артефакт — создавать по шаблону после подтверждения, отказывать в путях вне корня; проверка — дымовой тест открывает существующий артефакт и получает отказ на `../../etc/passwd`
- [ ] 3.5 Реализовать диагностику валидации: перевод замечаний в диагностики, запуск по сохранению файла change и по команде, очистку исправленного; проверка — модульные тесты на замечание со строкой, без строки и без файла, дымовой тест проверки change с ошибкой на фикстуре
- [ ] 3.6 Реализовать панель разделов: одна на окно, HTML с CSP и nonce, переписывание адресов ресурсов, мост сообщений к встраиваемому бэкенду и пересылка событий, навигация; проверка — модульный тест HTML панели (CSP без сети, nonce у каждого скрипта, ресурсы только из `media/web`), дымовой тест кругового запроса `/api/workspace` через `postMessage` и отказа на путь вне `/api/`
- [ ] 3.7 Реализовать команды палитры и строку состояния: обновить, создать change с выбором схемы, проверить, архивировать, открыть разделы; проверка — дымовой тест регистрирует все команды из манифеста и создаёт change командой на копии фикстуры

## 4. Упразднение отдельной IDE

- [ ] 4.1 Заменить бинарь скриптом разработки `scripts/serve.mjs`, перевести на него `npm run dev` и e2e, удалить `packages/cli`; проверка — `npm run verify` и `npm run test:e2e` проходят без `packages/cli`
- [ ] 4.2 Обновить README: установка расширения из `.vsix`, команды и разделы в VS Code, разработка; проверка — по README из чистого клона собирается `.vsix` командой `npm run package`

## 5. Приёмка

- [ ] 5.1 Пройти вручную сценарии `specs/vscode-extension/spec.md` в настоящем VS Code 1.90+ на фикстурах `full-change` и `custom-schema` и записать результат; проверка — таблица «сценарий → результат» в описании PR
