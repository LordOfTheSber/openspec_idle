# Tasks

## 1. Каркас проекта

- [x] 1.1 Поднять npm workspaces с пакетами `core`, `server`, `web`, `cli`, общим TypeScript в строгом режиме и общей конфигурацией сборки; проверка — `npm run build` собирает все четыре пакета без ошибок типов
- [x] 1.2 Завести команды `npm run verify` (типы + линт + модульные тесты) и `npm run dev` (сервер и Vite одной командой); проверка — `npm run verify` проходит на пустом проекте, `npm run dev` отдаёт страницу
- [x] 1.3 Собрать фикстурные OpenSpec-проекты для тестов: пустой, с одним change без артефактов, с полным change, с архивом и с собственной схемой, намеренно непохожей на `spec-driven` (другие имена артефактов, лишний этап, другой отслеживаемый артефакт); проверка — тест перечисляет фикстуры и подтверждает, что `openspec validate` на каждой даёт ожидаемый исход
- [x] 1.4 Реализовать бинарь `openspec-ide` с разбором пути, `--port`, `--no-open` и поиском ближайшего `openspec/` вверх по дереву; проверка — тесты на запуск из подкаталога, на занятый порт (ненулевой код и внятное сообщение) и на проект без `openspec/`

## 2. Обёртка над CLI OpenSpec

- [ ] 2.1 Реализовать модуль `openspecCli`: запуск процесса, таймаут, возврат кода, разобранного JSON и исходного вывода при ошибке разбора; проверка — тесты на успешный вызов, ненулевой код и невалидный JSON, где ошибка разбора не выдаётся за результат
- [ ] 2.2 Описать схемы ответов `list`, `list --specs`, `show`, `status`, `validate`, `instructions`, `templates` и разбирать вывод через них; проверка — тесты на фикстурах сверяют разобранные структуры с реальным выводом установленной версии CLI
- [ ] 2.3 Добавить кэш результатов по времени изменения файлов change и его сброс при изменениях на диске; проверка — тест подтверждает, что повторный запрос без правок не порождает нового процесса, а правка файла кэш сбрасывает
- [ ] 2.4 Добавить в обёртку команды схем: `schemas --json`, `schema which --json`, `schema validate`, `schema init`, `schema fork`, `templates --schema --json`; проверка — тесты на фикстурах сверяют разобранные ответы с выводом CLI, включая проектную схему, затеняющую встроенную
- [ ] 2.5 Проверить отсутствие CLI OpenSpec и отдавать блокирующее состояние с названием инструмента и командой установки; проверка — тест с подменённым PATH получает это состояние вместо исключения

## 3. Сервер, транспорт и безопасность

- [ ] 3.1 Поднять Fastify на loopback с генерацией токена сессии при старте и его проверкой на каждом запросе к API; проверка — тесты на запрос без токена (401) и на то, что токен различается между двумя запусками
- [ ] 3.2 Реализовать канонизацию путей с разрешением символических ссылок и отказ в операциях вне корня; проверка — тесты на переход выше корня и на символическую ссылку наружу, оба получают отказ, файл не читается и не изменяется
- [ ] 3.3 Реализовать поток SSE с переподключением и нарастающей паузой на стороне страницы; проверка — e2e-тест обрывает поток и подтверждает показ признака отключения, восстановление и полное перечитывание состояния
- [ ] 3.4 Подключить наблюдение за `openspec/` с объединением событий в одном окне; проверка — тест на пакетное изменение десятков файлов даёт одно обновление, тест на внешнюю правку файла — обновление в пределах двух секунд
- [ ] 3.5 Реализовать чтение и запись `.openspec-ide/config.json` с сохранением нераспознанных полей и отказом записывать значения секретов; проверка — тесты на отсутствующий файл, на сохранение неизвестного поля при перезаписи и на отклонение записи секрета

## 4. Обозреватель рабочего пространства

- [ ] 4.1 Собрать оболочку SPA: рельс разделов, панели, статус-строка, тёмная и светлая темы, раскладка до ширины телефона; проверка — e2e-тест открывает страницу на ширине 400 и 1280 пикселей и подтверждает отсутствие горизонтальной прокрутки страницы
- [ ] 4.2 Реализовать дерево changes, спеков и архива с вложенными путями capability; проверка — тест на фикстуре с путём `identity/user-auth` подтверждает вложенность и отсутствие схлопывания одноимённых последних сегментов
- [ ] 4.3 Реализовать индикаторы состояния артефактов и прогресс задач в дереве; проверка — тесты на артефакт «отсутствует», артефакт с ошибкой валидации и прогресс `4/11` на фикстуре
- [ ] 4.4 Реализовать поиск по changes, capability, схемам, требованиям и сценариям с фильтром по типу и переходом к строке; проверка — тесты на поиск по тексту требования, на пустую выдачу и на фильтр «только сценарии»
- [ ] 4.5 Реализовать раздел «Процессы» в дереве и раскрытие артефактов change по его схеме; проверка — на фикстуре с непохожей схемой дерево показывает именно её артефакты, а не фиксированный перечень

## 5. Редактор артефактов

- [ ] 5.1 Встроить CodeMirror с подсветкой формата OpenSpec: заголовки операций, требования, сценарии, WHEN/THEN; проверка — тесты на разметку сценария тремя решётками и на требование без сценариев, оба помечаются в тексте
- [ ] 5.2 Реализовать атомарное сохранение через запись во временный файл с переименованием и определение внешнего изменения файла; проверка — тесты на сбой записи (исходный файл не изменился, правки в редакторе целы) и на расхождение с диском (сохранение блокируется, показывается сравнение)
- [ ] 5.3 Реализовать панель структуры документа с переходом к строке требования и сценария; проверка — e2e-тест выбирает элемент структуры и подтверждает позицию курсора в редакторе
- [ ] 5.4 Реализовать создание артефакта из шаблона схемы и вставку блоков требования и сценария; проверка — тест создаёт отсутствующий `design.md` и сверяет его с выводом `openspec templates`, а на фикстуре с непохожей схемой создаёт её собственный артефакт по её шаблону
- [ ] 5.5 Ограничить подсветку и структурные проверки OpenSpec артефактами, порождающими дельты спеков; проверка — тест на артефакте собственной схемы без спеков подтверждает обычную подсветку markdown и отсутствие замечания «нет требований»
- [ ] 5.6 Реализовать панель проверок по выводу `validate --strict --json` с отменой предыдущего запуска и переходом к источнику ошибки; проверка — тесты на отмену при двойном сохранении и на переход по записи в нужный файл и строку

## 6. Дельты и связи

- [ ] 6.1 Реализовать структурный просмотр спека по `show --type spec --json` со сворачиванием требований; проверка — тест на фикстуре сверяет число требований и сценариев с выводом CLI
- [ ] 6.2 Реализовать группировку дельт по операциям с проверкой полноты REMOVED и RENAMED; проверка — тесты на удалённое требование без причины и без миграции помечают требование неполным с указанием недостающего поля
- [ ] 6.3 Реализовать сравнение требования из MODIFIED с его версией в основном спеке; проверка — тесты на изменённое требование (видны добавленные и удалённые сценарии) и на требование, отсутствующее в основном спеке (предупреждение и список похожих заголовков)
- [ ] 6.4 Реализовать карту связей capability и changes с пометкой конфликтов и висячих путей; проверка — тесты на capability под двумя changes (пересекающиеся требования помечены) и на дельту с несуществующим путём

## 7. Доска процесса

- [ ] 7.1 Реализовать построение колонок доски из артефактов схемы change в порядке зависимостей и вычисление фазы из `status --json`; проверка — тесты раскладывают фикстуры по колонкам на `spec-driven` и на непохожей схеме, где колонок больше и они названы иначе
- [ ] 7.2 Реализовать объединение колонок для changes на разных схемах и показ имени схемы на карточке; проверка — тест с двумя фикстурными changes на разных схемах подтверждает единый набор колонок и попадание каждой карточки в колонку своей схемы
- [ ] 7.3 Реализовать выбор схемы при создании change со значением по умолчанию из настроек проекта; проверка — e2e-тест создаёт change по собственной схеме и подтверждает состав его артефактов
- [ ] 7.4 Реализовать карточку change со статусами артефактов, прогрессом, результатом валидации и временем изменения; проверка — тест на новый change показывает состояние проверки «не выполнялась»
- [ ] 7.5 Реализовать операции создания, валидации и архивации через CLI с подтверждением архивации невыполненного change; проверка — e2e-тесты на успешную архивацию (change уходит в архив, спеки обновляются), на недопустимое имя и на ненулевой код команды (состояние доски не меняется)
- [ ] 7.6 Реализовать переключение чекбокса пункта точечной правкой строки отслеживаемого артефакта; проверка — тест на файле с неровным форматированием подтверждает побайтовую неизменность остальных строк и пересчёт прогресса, а тест на непохожей схеме — запись в объявленный ею файл

## 8. Метрики по пунктам плана

- [ ] 8.1 Реализовать в `core` разбор отслеживаемого артефакта на пункты с группой, номером, текстом, позицией и признаком выполнения, определяя файл по `apply.tracks` схемы change; проверка — тест сверяет число выполненных с `completedTasks` из `openspec list --json` на всех фикстурах, включая `- [ x]`, строки без чекбокса и схему с другим именем отслеживаемого артефакта
- [ ] 8.2 Обработать схему без объявленного отслеживаемого артефакта: метрики пунктов не ведутся, интерфейс объясняет причину; проверка — тест на такой фикстуре подтверждает отсутствие метрик пунктов и наличие объяснения вместо пустого экрана
- [ ] 8.3 Реализовать устойчивые идентификаторы задач и перенос метрик при перенумерации по порогу схожести текста; проверка — тесты на переформулировку (метрики сохранены), на перенумерацию (метрики перенесены, запись в журнале) и на удаление задачи (метрики помечены как относящиеся к удалённой и исключены из сводки)
- [ ] 8.4 Реализовать журнал `.openspec-ide/runs.jsonl` и пересборку снимка `metrics.json`; проверка — тесты на восстановление снимка из журнала и на журнал с битой строкой (строка пропущена, остальные события применены)
- [ ] 8.5 Реализовать набор метрик пункта плана: состояние, времена, запуски и их исходы, токены, вызовы инструментов, затронутые файлы; проверка — тесты на незапускавшуюся задачу (нули и отсутствующие отметки), на завершение (время цикла) и на возврат в работу (счётчик возвратов, сохранённый расход)
- [ ] 8.6 Реализовать извлечение критерия приёмки из текста задачи, запуск привязанной команды проверки и признак расхождения; проверка — тесты на задачу без выделяемого критерия, на нулевой и ненулевой код возврата и на отметку выполнения при красной проверке
- [ ] 8.7 Реализовать сводку change и экран метрик с сортировкой по расходу; проверка — тесты на отсутствие данных (показатели расхода не подменяются нулём) и на сортировку (задачи без данных в конце списка)
- [ ] 8.8 Реализовать экспорт метрик change и всего рабочего пространства с версией схемы данных; проверка — тест сверяет выгрузку со схемой и подтверждает пометку архивных changes

## 9. Конструктор процессов и SDD-соответствие

- [ ] 9.1 Реализовать реестр схем: перечень, источник разрешения, затенение, схема проекта по умолчанию, нечитаемая схема; проверка — тесты на проект без собственных схем, на проектную схему, затеняющую встроенную, и на схему с некорректным YAML (помечена нечитаемой, остальные доступны)
- [ ] 9.2 Реализовать создание схемы с нуля и форком с проверкой занятости имени; проверка — тесты на `schema init`, на `schema fork` (артефакты, шаблоны и инструкции перенесены) и на занятое имя
- [ ] 9.3 Реализовать три синхронных представления схемы — граф, форму свойств артефакта и YAML; проверка — тесты на правку в каждом из трёх представлений подтверждают отражение в двух других, а тест на временно некорректный YAML — показ последнего корректного состояния как устаревшего и блокировку сохранения
- [ ] 9.4 Реализовать удаление артефакта с разбором зависимостей и правку шаблонов артефактов; проверка — тесты на удаление артефакта, от которого зависят другие (список зависящих, снятие зависимостей после подтверждения) и на объявленный, но отсутствующий файл шаблона
- [ ] 9.5 Реализовать в `core` правила SDD-соответствия по полям схемы: наличие артефакта-контракта, зависимость начала работ от него, объявленность и существование отслеживаемого артефакта, достижимость артефактов, мотивация перед спеками, заполненность инструкций; проверка — модульные тесты на каждое правило по отдельной схеме-фикстуре, плюс тест «встроенная `spec-driven` не нарушает ничего» и тест «схема из `schema init` даёт только предупреждения об инструкциях»
- [ ] 9.6 Реализовать отчёт о соответствии с перечнем пройденных правил и переходом от нарушения к артефакту на графе; проверка — e2e-тест выбирает нарушение и подтверждает выделение артефакта и открытие нужного поля формы
- [ ] 9.7 Реализовать двухслойную проверку схемы: структурную через `openspec schema validate` и SDD-слой, с различением слоя в отчёте; проверка — тесты на циклическую схему (структурный слой), на схему без спеков (слой SDD) и на схему, проходящую оба слоя
- [ ] 9.8 Реализовать адресный отказ от правила в самой схеме с обязательной причиной; проверка — тесты на отказ с причиной (правило не блокирует, показано отклонённым), на отказ без причины (не применяется) и на неизвестный идентификатор правила (предупреждение)
- [ ] 9.9 Реализовать назначение схемы проекту и блокировку назначения при нарушениях уровня «ошибка» при сохраняемой возможности сохранить черновик; проверка — тесты на обновление поля схемы в настройках без изменения остального файла, на отказ в назначении с перечнем нарушений и на сохранение несоответствующей схемы с пометкой в реестре
- [ ] 9.10 Реализовать предпросмотр процесса: порядок артефактов, колонки доски, отслеживаемый артефакт; проверка — тест на схеме без отслеживаемого артефакта показывает предупреждение о неизмеримом прогрессе

## 10. Мост к агенту на GigaCode CLI

- [ ] 10.1 Сверить фактический интерфейс установленной сборки GigaCode CLI с унаследованным от Qwen Code и зафиксировать значения по умолчанию в описании запуска; проверка — записанный в репозиторий вывод `gigacode --help` и тест, сверяющий с ним шаблон аргументов
- [ ] 10.2 Реализовать настройки подключения: путь, модель, режим, бюджеты, имя переменной окружения; проверка — тесты на сохранение без значения секрета, на отклонение ввода секрета и на незаданную переменную окружения (запуск заблокирован с указанием переменной)
- [ ] 10.3 Реализовать пробу доступности с определением версии и поддержки формата вывода и переход на одноразовый вывод; проверка — тесты на отсутствующий исполняемый файл (перечислены проверенные пути) и на сборку без потокового вывода (переход с уведомлением)
- [ ] 10.4 Реализовать сборку промпта из `instructions --json` с контекстом пункта плана и правкой перед запуском; проверка — тесты на запуск по артефакту, на запуск по задаче (текст и критерий приёмки в промпте) и на ошибку команды инструкций
- [ ] 10.5 Реализовать запуск процесса, нормализующий разбор потока событий и вывод в панель агента; проверка — тесты на записанных потоках событий, включая неизвестный тип события (сохранён как есть, обработка продолжена) и оборванную последнюю строку
- [ ] 10.6 Реализовать бюджеты времени и вызовов инструментов и отдельные исходы их превышения; проверка — тесты на превышение каждого предела сохраняют собранные показатели и помечают исход
- [ ] 10.7 Реализовать режимы подтверждения с безопасным значением по умолчанию и явным согласием на полное автоподтверждение; проверка — e2e-тесты на режим по умолчанию, на согласие с предупреждением и на режим анализа без изменения файлов
- [ ] 10.8 Реализовать остановку запуска, блокировку параллельного запуска по change и журнал запусков со скрытием секретов; проверка — тесты на остановку (мягкий сигнал, затем принудительный, показатели сохранены), на параллельный запуск (ссылка на выполняющийся) и на вырезание значения секрета из сохранённой записи
- [ ] 10.9 Связать записи о запусках с метриками задач и сводкой change; проверка — тесты на запуск по задаче (показатели в метриках задачи) и без привязки (только в сводке change)

## 11. Приёмка

- [ ] 11.1 Прогнать сквозной сценарий на чистом проекте: инициализация, создание change, заполнение артефактов, запуск агента с заглушкой CLI, отметка задач, архивация; проверка — e2e-тест проходит целиком, а `openspec validate --strict` на итоговом проекте не находит замечаний
- [ ] 11.2 Свести соответствие экранов требованиям спецификации и закрыть непокрытые; проверка — таблица «требование → тест» покрывает все требования девяти capability
- [ ] 11.3 Прогнать сквозной сценарий на собственном процессе: собрать схему в конструкторе, довести её до соответствия SDD, назначить проекту, завести по ней change и пройти его до архивации; проверка — e2e-тест проходит целиком, `openspec schema validate` на созданной схеме и `openspec validate --strict` на итоговом проекте не находят замечаний
- [ ] 11.4 Написать README с установкой, запуском, настройкой GigaCode CLI и разделом о построении собственного процесса; проверка — установка по README на чистой машине доводит до открытого интерфейса и успешной пробы подключения
