# Tasks

## 1. Промпт и протокол

- [x] 1.1 Добавить необязательный замысел в цель запуска по артефакту: разбор в маршрутах `/api/agent/prompt` и `/api/agent/run`, раздел «Замысел автора» в промпте, ограничение 4000 символов; проверка — тесты сборщика промпта на промпт с замыслом, без замысла и с обрезкой длинного замысла
- [x] 1.2 Добавить поле `agent` в сообщение навигации панели с проверкой артефакта и замысла; проверка — тесты протокола принимают корректную цель и отклоняют `../` и нестроковый замысел

## 2. Интерфейс

- [x] 2.1 Раздел «Агент» принимает артефакт и замысел, собирает промпт сразу, поле замысла перед списком артефактов, режим `auto-edit` вместо `plan`/`default` с пояснением; проверка — e2e: переход «Сгенерировать» открывает подтверждение запуска по артефакту с замыслом в промпте и режимом `auto-edit`
- [x] 2.2 «Сгенерировать агентом» в панели деталей change на доске, во встроенном редакторе отсутствующего артефакта и замысел в форме создания change; проверка — e2e: создание change с замыслом открывает агента по proposal, кнопка у несозданного design открывает агента по design

## 3. VS Code

- [x] 3.1 Выбор «Сгенерировать агентом / Пустой по шаблону» у отсутствующего артефакта в дереве и замысел в команде «Создать change»; проверка — дымовой тест: выбор генерации открывает панель, после `ready` панель получает навигацию на раздел «Агент» с артефактом и замыслом

## 4. Документация

- [x] 4.1 README: когда используется CLI OpenSpec и когда агент, генерация артефактов; проверка — `npm run verify` и `npm run test:e2e` проходят
