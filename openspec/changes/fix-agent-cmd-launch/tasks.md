# Tasks

## 1. Запуск на Windows

- [x] 1.1 Разбор обёрток `.cmd`: `%~dp0` без косой черты, `SET`, пути без кавычек, `call`, свой `node.exe`, флаги и аргументы перед `%*`, вызов `.exe`; проверка — тесты `platform.test.ts` на обёртку npm, установщика со своим Node, с переменными, с `.exe` и на нераспознаваемые обёртки
- [x] 1.2 Запасной запуск через `cmd.exe`: закавычивание аргументов, отказ на переводе строки, кавычке и `%`, промпт на стандартный ввод, пометка в проверке подключения; проверка — тест плана запуска для `win32`, тест передачи стандартного ввода, тесты агента проходят

## 2. Документация

- [x] 2.1 README: какие обёртки разбираются и что происходит с нераспознанной; проверка — `npm run verify` проходит
