# Tasks: модули монорепозитория

## 1. Модель в core

- [x] 1.1 Реализовать модель карты модулей, её разбор и проверку: неизвестная зависимость, совпадающие префиксы, пустые поля, цикл зависимостей; проверка — модульные тесты на каждую ошибку карты с указанием модуля и поля, плюс тест «ошибка одного модуля не ломает остальные»
- [x] 1.2 Реализовать сопоставление capability модулю по самому длинному префиксу целых сегментов и модули change по дельтам и явному списку; проверка — тесты на вложенные префиксы, `km/core` против `km/core-legacy`, change без дельт и capability вне модулей
- [x] 1.3 Реализовать граф модулей и затронутых потребителей с глубиной и защитой от цикла; проверка — тесты на прямых и транзитивных потребителей библиотеки и на карту с циклом

## 2. Сервер

- [x] 2.1 Читать и записывать `openspec/modules.yaml` с проверкой каталогов кода; проверка — тесты чтения корректной и ошибочной карты и записи без потери неизвестных ключей
- [x] 2.2 Реализовать обнаружение модулей по `package.json`, `pom.xml`, `build.gradle(.kts)`, `go.mod`, `*.csproj`, `pyproject.toml` с зависимостями и пропуском игнорируемых каталогов; проверка — фикстура монорепо (5 сервисов, UI, КМ из 4 библиотек, npm и Maven) даёт ожидаемые модули и зависимости, `node_modules` и `target` не обходятся
- [x] 2.3 Показывать при повторном обнаружении только расхождения с существующей картой; проверка — тест: добавленный в код модуль попадает в черновик, правки карты не перезаписываются
- [x] 2.4 Добавить модули в ответы дерева, доски, поиска и деталей change и сводку метрик по модулю; проверка — тесты API на фикстуре со сквозным change
- [x] 2.5 Записывать ключ `modules` в `.openspec.yaml` при создании change, не трогая остальное содержимое; проверка — тест побайтового сравнения остальных строк и `openspec validate --strict` после записи

## 3. Интерфейс

- [x] 3.1 Раздел «Модули»: список по группам, граф зависимостей, панель модуля с зависимостями, потребителями, changes и спеками; проверка — e2e-тест выбора модуля на фикстуре
- [x] 3.2 Экран обнаружения: черновик с правкой вида, названия, префикса и группы, удалением модуля и сохранением; проверка — e2e-тест от пустой карты до сохранённого `openspec/modules.yaml`
- [x] 3.3 Дерево по модулям с группой «вне модулей» и отметкой сквозных changes; проверка — e2e-тест: сквозной change виден под обоими модулями
- [x] 3.4 Фильтр по модулю на доске, в поиске и метриках, общий для разделов; проверка — e2e-тест: фильтр `billing` оставляет его changes и сквозные и сохраняется при переключении разделов
- [x] 3.5 Затронутые потребители в деталях change и подсветка на графе; проверка — e2e-тест change в библиотеке КМ подсвечивает прямых и транзитивных потребителей
- [x] 3.6 Выбор модулей при создании change и подсказка префикса при добавлении дельты, дельты по модулям; проверка — e2e-тест создания сквозного change и добавления дельты в модуль

## 4. Приёмка

- [x] 4.1 Прогнать сквозной сценарий на фикстуре монорепо: обнаружить модули, сохранить карту, создать сквозной change в библиотеке и сервисе, увидеть затронутых потребителей, отфильтровать доску, архивировать; проверка — e2e-тест проходит целиком, `openspec validate --strict` чист
- [x] 4.2 Описать в README карту модулей и обнаружение; проверка — по README карта заводится на новом репозитории
