# Tasks: спека модуля, связи между модулями и с кодом

## 1. Модель в core

- [x] 1.1 Реализовать разбор разделов из имени требования с вложенностью, разделом «Общее» и сохранением порядка файла; проверка — модульные тесты на разделы, вложенные разделы, требования без разделителя и имена с косой чертой без пробелов (не раздел)
- [x] 1.2 Реализовать проверку формата спеки модуля: заголовок `##` после Requirements с числом потерянных требований, повтор имени, разделы, различающиеся регистром или пробелами; проверка — тесты на каждый случай плюс сверка числа требований с `openspec show --json` на фикстуре
- [x] 1.3 Реализовать якорь требования по правилам GitHub и разбор ссылок между спеками с относительными путями, в том числе из дельты change; проверка — тесты якорей для кириллицы, знаков препинания и ` / `, и тест, что ссылка из дельты разрешается до и после архивации
- [x] 1.4 Реализовать разбор меток `@spec` в строке любого языка с отрезанием хвоста комментария и классификацию файла как теста по шаблонам; проверка — тесты на `//`, `#`, `/* */`, `<!-- -->`, `--`, на тестовые пути Maven, Gradle, Jest, Go и Python
- [x] 1.5 Реализовать нормализацию имён сценариев и поиск вероятного покрытия в тестовом тексте; проверка — тесты на имя теста в обратных кавычках, `@DisplayName`, строковый литерал и на отсутствие совпадения в нетестовом файле

## 2. Сервер

- [x] 2.1 Отдавать структуру спеки модуля: разделы, требования, сценарии, обзор модуля, признаки активных changes по дельтам; проверка — тесты API на фикстуре монорепо со спекой из трёх разделов и активным MODIFIED
- [x] 2.2 Собирать историю требования по дельтам архива с цепочкой RENAMED; проверка — фикстура с двумя архивными изменениями и переименованием даёт полную историю
- [x] 2.3 Строить граф ссылок между требованиями: исходящие, обратные, битые, расхождения с картой модулей; проверка — тесты на обратные ссылки, битую ссылку после переименования с предложенной заменой и на ссылку без зависимости в карте
- [x] 2.4 Строить индекс меток в фоне по каталогам кода модулей с пропуском игнорируемых файлов, кэшем в `.openspec-ide/code-index.json` и инкрементальным обновлением по наблюдателю; проверка — тест: повторное открытие перечитывает только изменённые файлы, добавленная метка появляется без полной переиндексации, `node_modules` и `target` не читаются
- [x] 2.5 Считать покрытие требований и спеки и находить битые метки с предложением нового имени; проверка — тесты на все состояния покрытия и на метку со старым именем после RENAMED
- [x] 2.6 Отдавать фрагмент файла вокруг строки и запускать внешний редактор IDEA или VS Code по настройке с разбором обёрток `.cmd`; проверка — тест запуска на заглушке редактора (аргументы файла и строки) и тест «команда не найдена» с перечнем путей

## 3. Интерфейс

- [x] 3.1 Экран спеки модуля: обзор, оглавление разделов со счётчиками, карточки требований со сценариями, поиск внутри спеки, прямой адрес требования; проверка — e2e-тест на фикстуре: переход по оглавлению, поиск по тексту сценария, открытие требования по адресу
- [x] 3.2 Признаки на карточке: активный change со сравнением текстов, покрытие, число исходящих и входящих ссылок; история требования с различиями; проверка — e2e-тест открытия сравнения и истории
- [x] 3.3 Ссылки в тексте требования с превью цели, обратные ссылки, вставка ссылки из редактора выбором требования; проверка — e2e-тест вставки ссылки в спеку и появления обратной ссылки у цели
- [x] 3.4 Панель трассировки у требования: реализация и тесты с фрагментом, «по имени» отдельно, открытие в редакторе; фильтр «без покрытия» в обзоре; проверка — e2e-тест на фикстуре с метками в Kotlin и TypeScript
- [x] 3.5 Связи требований на графе модулей с числом ссылок и расхождениями с картой; битые ссылки и метки в деталях change; проверка — e2e-тест на расхождение «ссылка без зависимости»
- [x] 3.6 Настройка внешнего редактора и шаблонов тестовых путей; проверка — e2e-тест сохранения настройки и открытия места через заглушку редактора

## 4. Приёмка

- [x] 4.1 Сквозной сценарий на фикстуре монорепо: спека сервиса с разделами, ссылка на библиотеку КМ, метки в коде и тестах, change с MODIFIED и RENAMED — видны затронутые требования и метки к обновлению, после архивации история и покрытие верны; проверка — e2e-тест проходит целиком, `openspec validate --strict` чист
- [x] 4.2 Описать в README соглашения: раздел в имени требования, формат ссылки, метка `@spec`, шаблоны тестов; проверка — по README команда оформляет спеку и метки без дополнительных пояснений
