# Tasks

## 1. Ядро: сравнение спеков

- [x] 1.1 Реализовать в `packages/core/src/textDiff.ts` построчное сравнение (LCS с отсечением общего начала и конца) и нарезку на фрагменты с тремя строками контекста; проверка — тесты на вставку, удаление, замену, одинаковые тексты, пустой текст «до» и защиту от слишком большой матрицы, тест чистоты `core` проходит
- [x] 1.2 Реализовать в `packages/core/src/specChange.ts` изменения спека по требованиям: добавлено, изменено, переименовано (по парам RENAMED дельты), удалено, без изменений, появившиеся и пропавшие сценарии изменённого требования; проверка — тесты на новую capability, переименование с правкой текста, удаление и изменённое требование с новым сценарием

## 2. Бэкенд: предпросмотр архивации

- [x] 2.1 Реализовать `ArchivePreviewService` в `packages/server`: временная копия `openspec/` без чужих changes и архива, прогон `openspec archive <name> --yes --json` с отключённой телеметрией, исходы `ready` / `validation-failed` (второй прогон с `--no-validate`) / `refused`, сравнение спеков, удаление копии; проверка — тесты на фикстурах `full-change` (новая capability, исход `ready`), `delta-ops` (`refused` с именем потерянного сценария) и change с ошибкой проверки (`validation-failed` со спеками), файлы проекта побайтово не меняются, временных каталогов не остаётся
- [x] 2.2 Добавить маршрут `GET /api/archive/preview?change=` с 404 для неизвестного change и пути первых файлов артефактов в карточки доски; проверка — тест маршрута через `inject` (200 и 404) и тест доски на путь `proposal.md` в карточке

## 3. Интерфейс: доска и предпросмотр

- [x] 3.1 Компонент предпросмотра архивации: исход и причина, сводка по capability, требования с отметками, сценарии, сворачиваемое построчное сравнение, кнопка «Открыть сравнение» в VS Code; вкладка «После архивации» в разделе «Дельты»; проверка — e2e на `full-change` показывает новую capability `data-export` и исход «пройдёт», на `delta-ops` — отказ с сообщением CLI
- [x] 3.2 Новая раскладка доски: без дерева, сворачивание пустых колонок, режимы «Авто / Колонки / Список» с сохранением выбора, фильтр по имени, время изменения и переходы по меткам артефактов на карточке; проверка — e2e: пустая колонка свёрнута и разворачивается, при ширине окна 560 px доска показана списком, фильтр оставляет одну карточку
- [x] 3.3 Панель деталей change: действия «Проверить», «Дельты», «Метрики», «Агент», «Архивировать», артефакты с переходом, пункты по группам, результат проверки с переходом к строке, закрытие по Esc; подтверждение архивации с предпросмотром и недоступной кнопкой при отказе; проверка — e2e: «Метрики» открывает раздел метрик change, архивация `full-feature` показывает предпросмотр и после подтверждения change уходит с доски, у `rework-export` кнопка архивации недоступна
- [x] 3.4 Живое обновление доски, деталей и предпросмотра по событию изменения файлов; оформление кнопок, полей и списков по теме VS Code; проверка — e2e: правка `tasks.md` на диске в обход IDE переводит карточку в «Готово к архивации» без действий в интерфейсе

## 4. Расширение VS Code

- [x] 4.1 Провайдер виртуальных документов `openspec-preview`, сравнение текущего спека и спека после архивации, команда «Предпросмотр архивации» в палитре и контекстном меню change, сообщение панели `preview-archive` в протоколе; проверка — тесты протокола (корректное и некорректное сообщение), дымовой тест: команда на копии `full-change` открывает `vscode.diff` с пустой левой стороной и правой стороной, содержащей требование дельты
- [x] 4.2 Сводка предпросмотра в подтверждении архивации и отказ без предложения архивировать при исходе, отличном от «пройдёт»; снятие диагностик changes, исчезнувших с доски; проверка — модульный тест текста сводки, дымовой тест архивации `full-change` командой

## 5. Макеты и документация

- [x] 5.1 Перерисовать в `docs/mockups/openspec-ide.html` экран «Доска» в окне VS Code (колонки со свёрнутыми пустыми, панель деталей, режим «Список») и добавить экран «Предпросмотр архивации»; проверка — макет открывается без ошибок в консоли, экраны переключаются, оба экрана перечисляют требования этого change
- [x] 5.2 Обновить README: предпросмотр архивации, доска в VS Code, новая команда; проверка — описанные команды совпадают с манифестом расширения, `npm run verify` и `npm run test:e2e` проходят
