# Tasks: десктопное приложение

## 1. Каркас приложения

- [x] 1.1 Завести пакет `packages/desktop` на Electron с главным процессом, который поднимает `startServer` и открывает окно на его адресе; проверка — e2e-тест через Playwright `_electron` запускает приложение на фикстурном проекте и видит дерево changes
- [x] 1.2 Отделить сборку приложения от `npm run build`: отдельные скрипты `desktop:dev` и `desktop:dist`; проверка — `npm run build` и `npm run verify` проходят без установленного двоичного Electron

## 2. Окна и жизненный цикл

- [x] 2.1 Реализовать одно окно на репозиторий с именем репозитория в заголовке и фокус на существующем окне при повторном открытии; проверка — e2e-тест открывает тот же репозиторий дважды и получает одно окно
- [x] 2.2 Реализовать блокировку второго экземпляра с передачей пути работающему экземпляру; проверка — тест второго запуска с путём открывает репозиторий в первом экземпляре
- [x] 2.3 Сохранять и восстанавливать размер и положение окна по пути репозитория с переносом на основной монитор; проверка — модульный тест восстановления геометрии, включая положение вне экранов
- [x] 2.4 Останавливать сервер окна при его закрытии и запрашивать подтверждение при выполняющемся запуске агента; проверка — e2e-тест закрытия с выполняющимся запуском заглушки: отказ оставляет окно, подтверждение помечает запуск прерванным

## 3. Стартовый экран

- [x] 3.1 Реализовать стартовый экран с открытием папки системным диалогом и поиском корня вверх по дереву; проверка — e2e-тест с подменённым диалогом открывает вложенную папку фикстуры и получает корень
- [x] 3.2 Реализовать список недавних с пометкой недоступных и удалением из списка; проверка — модульный тест хранилища недавних и e2e-тест на удалённую папку
- [x] 3.3 Предлагать `openspec init` для папки без OpenSpec; проверка — e2e-тест на пустой папке: после согласия открывается рабочее пространство, `openspec/` создан

## 4. Встроенный CLI и безопасность окна

- [x] 4.1 Использовать CLI из репозитория, иначе встроенный через `ELECTRON_RUN_AS_NODE`, и показывать источник и версию в строке состояния; проверка — тесты на репозиторий со своей версией и на репозиторий без неё
- [x] 4.2 Открывать внешние ссылки в системном браузере и запрещать навигацию окна за пределы своего сервера; проверка — e2e-тест перехода по внешней ссылке: окно остаётся на IDE, вызвано открытие во внешнем браузере

## 5. Меню и горячие клавиши

- [x] 5.1 Реализовать меню «Файл», «Вид», «Справка» с недавними и переключением разделов, сочетания Ctrl+O и клавиши разделов; проверка — e2e-тест вызывает пункты меню и сочетания и проверяет результат

## 6. Поставка

- [ ] 6.1 Настроить electron-builder: NSIS для пользователя, переносимая сборка, `dmg`, `AppImage`, встроенный CLI в ресурсах; проверка — сборка под Windows создаёт установщик и переносимый `.exe`, установка без прав администратора запускает приложение со стартовым экраном
- [ ] 6.2 Описать в README установку приложения, зеркало Electron для закрытой сети и сборку установщика; проверка — установка по README на чистой машине Windows доводит до открытого репозитория
