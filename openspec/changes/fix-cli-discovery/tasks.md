# Tasks

## 1. Бэкенд

- [x] 1.1 Поиск CLI: явный путь (`cliPath`, `OPENSPEC_CLI`) с разбором путей Git Bash, `~`, кавычек и PATHEXT; каталоги глобальной установки npm; имена переменных окружения без учёта регистра на Windows; подсказка в уведомлении; проверка — тесты `locate.test.ts` на каждый источник, на неверный явный путь и на пути Windows

## 2. VS Code и панель

- [x] 2.1 Настройка `openspec.cliPath`, перезапуск бэкенда при её изменении, повторный поиск по «Обновить», сообщение с кнопкой «Указать путь к CLI», подсказка в дереве, строке состояния и панели; проверка — дымовой тест: неверный путь даёт «нет CLI» с подсказкой, исправленный путь переводит расширение в «готово» без перезапуска

## 3. Документация

- [x] 3.1 README: порядок поиска CLI и что делать, если `openspec` работает только в терминале; проверка — `npm run verify` проходит
